# GitHub Pages Configuration
# This file is kept for reference but Jekyll is disabled via .nojekyll
# For static HTML sites, GitHub Pages will serve files directly
title: DTalk Support
description: Support page for DTalk app

